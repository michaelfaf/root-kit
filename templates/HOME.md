<!--
TEMPLATE — HOME.md, the map.

WHAT THIS FILE IS: where things live. Nothing else.
WHAT IT IS NOT: how things work (that's the standing-instructions file and per-folder READMEs),
or what's currently happening (that's project STATUS files).

THE DEPTH-2 CEILING: list top-level folders and root files, plus the one or two load-bearing
files inside a folder that a session would otherwise hunt for. Never leaf notes. A map that
lists every note is not a map, it's a directory listing, and it is stale the day after you
write it.

THE HONESTY RULE: this file routes, it never copies. The moment it starts summarizing what's
inside a folder, it has started lying — because the folder will change and this won't.

Frontmatter: keep it if your tools use it (Obsidian, static site generators), delete it if
they don't. Links: [[wikilinks]] if you're in Obsidian, [relative](paths.md) otherwise (DP-6).

Delete every comment block before you install.
-->

---
type: map
read_first: Home.md
updated: <YYYY-MM-DD>
---

# <Workspace name> — map

Where things live. Top-level folders and load-bearing files only, never leaf notes. Each entry routes onward.

## Start here — root files

- `<your standing-instructions file: CLAUDE.md or AGENTS.md, per DP-1>` · how the AI operates — read every session
- `Goals.md` · targets, priorities, cadence
- `Business.md` · <the business> — model, customers, pricing
- `About-me.md` · coaching, behavioral, decision patterns
- <`GUIDE.md` or similar, if you have one · the manual — how this workspace works. This page is the *map* (where things are); the guide is the *manual* (how they work).>

## Folders

<!--
One line each. Format: bold name · what it holds · where it routes next.
A folder with a README routes to the README, not to the folder.
A folder that needs no explanation gets one clause, not a paragraph.
A folder nobody ever navigates to on purpose (dated logs, attachments) gets a line saying
so, or gets left off entirely — say which, don't leave the reader wondering.
-->

- **`<01 Folder>/`** · <what lives here> → `<Folder>/README.md`
- **`<02 Folder>/`** · <what lives here> → `<Folder>/README.md`
- **`<03 Folder>/`** · <what lives here — no README, self-describing>
- **`<Archive or dated logs>/`** · <one line; note that it's not linked from anywhere and that's deliberate>

## <Sub-map for your biggest folder>

<!--
OPTIONAL — add this only when one folder is big enough that "→ its README" isn't enough
routing to be useful. Give the split, not the contents. Delete this whole section otherwise.
-->

Routes through `<Folder>/README.md`. The split:
- `<sub>` · `<sub>` · `<sub>`

## Key routes

<!--
The half-dozen questions that come up constantly and the exact file that answers each.
This is the section that earns its keep — it's a shortcut past the folder tree for the
things you look up weekly. If you never use a line here, delete it.
-->

- <Recurring question> → `<exact file>`
- <Recurring question> → `<exact file>`
- <Recurring question> → `<exact file>`
